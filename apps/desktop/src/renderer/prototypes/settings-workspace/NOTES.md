# Settings, Workspace, and Provider design gate

- A is a conventional destination page.
- B is a compact mission-adjacent inspector.
- C is a task-oriented readiness path with three guided checks.

**Selected: C — Guided setup.** It reduces dropdowns, explains what each check enables, preserves
native folder identity and revocation, shows provider-specific readiness, keeps degraded-storage
blocking visible, and ends with application evidence. B's compact attention summary may be reused
from mission context without exposing mutation controls there.
